<UserControl x:Class="DawsonDialGUI.Views.RoomMenuView"
             xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:DawsonDialGUI.ViewModels"
             x:DataType="vm:RoomMenuViewModel">
    <Grid>
        <Border Background="#1E1E1E"
                CornerRadius="12"
                Padding="30"
                BorderBrush="#444"
                BorderThickness="1"
                Width="400"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
            <StackPanel Spacing="15">
                <TextBlock Text="Room Menu"
                           FontSize="28"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           Foreground="White" />

                <Button Content="View All Rooms"
                        Command="{Binding ViewAllRoomsCommand}"
                        Classes="menu" />

                <Button Content="Add Room"
                        Command="{Binding AddRoomCommand}"
                        Classes="menu" />

                <Button Content="Back to Menu"
                        Command="{Binding GoBackToMenuCommand}"
                        Classes="menu" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>